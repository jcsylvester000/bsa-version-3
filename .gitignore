# dependencies
/node_modules
/.pnp
.pnp.js

# next
/.next/
/out/
next-env.d.ts

# production
/build

# secrets — never commit
.env
.env*.local
.env.production

# prisma
/prisma/*.db

# local object storage (dev)
/.storage

# misc
.DS_Store
*.pem
/coverage
*.log
npm-debug.log*

# editor
.vscode/*
!.vscode/extensions.json
.idea

# session scratch (do not publish)
/_to_delete
/.storage

tsconfig.tsbuildinfo

# Database dumps contain user password hashes + audit data — never commit.
*.dump
*.sql.gz

# PSGC boundary GeoJSON (large; owner-provided per R-02 README)
prisma/data/boundaries/*.geojson

# Census CSVs (owner-provided per R-04 README)
prisma/data/demographics/*.csv

# BIR zonal CSVs (owner-provided per R-05 README)
prisma/data/zonal/*.csv
